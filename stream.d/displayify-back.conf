# RTMPS (TLS)
upstream mediamtx_rtmps {
  server mediamtx.displayify.svc.cluster.local:1936;
}

server {
  listen 1936;
  proxy_pass mediamtx_rtmps;
  proxy_timeout 1h;
  proxy_connect_timeout 10s;
}
